<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.team4.mapper.sw.CartMapper">

    <!-- Cart 데이터 전체 가져오기(mno 단위) -->
    <select id="getAll" resultType="com.green.team4.vo.sw.CartVO">
        select mno,cno,tbl_cart.pno,tbl_product.pMainCategory,tbl_product.pSubCategory,
               tbl_product.pName,tbl_product.pPrice,tbl_product.pImage from tbl_cart
            left outer join tbl_product
                on tbl_cart.pno = tbl_product.pno
                    where mno=#{mno}
                        order by tbl_cart.cno
    </select>

    <!-- Cart 데이터 삭제 -->
    <delete id="delete">
        delete from tbl_cart where pno=#{pno}
    </delete>

</mapper>